---
title: Introduction
description: 'Getting Started on GL Cloud with Node.js'
---

## Introduction

This tutorial will have you deploying a Node.js app to GL Cloud in minutes.

The tutorial assumes that you have a [GL Cloud account](https://glcloud.app/login), and that you have [Node.js](https://nodejs.org/en/download/) and [npm](https://www.npmjs.com/package/npm) installed locally.

## Required Files

- index.js or index.ts (Example of main file).<br />
- package.json (dependency file).<br />
- GLcloud.app (file that contains the GL Cloud configuration).

## Preparing Files for Upload

Before hosting your project on GL Cloud, you must first have the files of your project properly configured, so that you can host it later.

It is recommended to **delete** the <code>node_modules</code> & **ffmpeg** folder, **package-lock.json** and **.npmrc** file, if it exists in your project.

Create your [glcloud.app]() configuration file.

After the files of your project are ready (and the unnecessary files deleted), just zip them in **.zip** format before uploading to GL Cloud.

​
## Uploading Files to GL Cloud
After preparing your project files, you can now upload them to GL Cloud and host your project. Access the [GL Cloud Dashboard](https://glcloud.app/dashboard) and upload your project files.
